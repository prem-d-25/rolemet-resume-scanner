IN EVN declate this :

    PORT=3001
    MONGO_URI=mongodb+srv:.....
    CLIENT_URL = Allowd client URL

    JWT_ACCESS_SECRET= key
    JWT_REFRESH_SECRET= key

    JWT_ACCESS_EXPIRES = in min 
    JWT_REFRESH_EXPIRES = in days 

    NODE_ENV=development
